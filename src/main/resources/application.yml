server:
  port: 8083

spring:
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

  application:
    name: tracker

  datasource:
    username: postgres
    password: postgres
    url: "jdbc:postgresql://localhost:5433/postgres?CurrentSchema=trackerrr"

  jpa:
    hibernate:
      ddl-auto: none